var Hoek = require('hoek');
var fs = require('fs');
var path = require('path');

module.exports = (request, reply) => {
  // var selectedTags = request.payload.tags;
  // console.log(selectedTags);
};
